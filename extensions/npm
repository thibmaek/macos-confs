#!/usr/bin/env bash

modules=(
  babel
  babel-eslint
  eslint
  forever
  greenkeeper
  grunt-cli
  gulp
  node-gyp
  nodemon
  npm
  speed-test
  vtop
  webpack
  yo
)

generators=(
  generator-devine-project
  generator-generator
)

npm install -g ${modules[@]}
npm install -g ${generators[@]}
