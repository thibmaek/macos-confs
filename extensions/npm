#!/usr/bin/env bash

modules=(
  eslint
  mklicense
  mocha
  nativefier
  node-gyp
  nodemon
  npm
  speed-test
  webpack
  webpack-validator
  yo
)

generators=(
  generator-devine-project
  generator-generator
)

npm install -g ${modules[@]}
npm install -g ${generators[@]}
