#!/usr/bin/env bash

modules=(
  commitizen
  eslint
  mklicense
  mocha
  nativefier
  node-gyp
  npm
  speed-test
  webpack
  yo
)

generators=(
  generator-devine-project
  generator-eslint-config
)

npm install -g ${modules[@]}
npm install -g ${generators[@]}
