#!/usr/bin/env bash

modules=(
  babel-cli
  bower
  eslint
  forever
  grunt-cli
  gulp
  node-gyp
  nodemon
  speed-test
  tmi
  uncss
  vtop
  webpack
  yo
)

generators=(
  generator-devine-project
  generator-generator
)

npm install -g ${modules[@]}
npm install -g ${generators[@]}
