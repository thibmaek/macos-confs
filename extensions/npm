#!/usr/bin/env bash

modules=(
  commitizen
  eslint
  mklicense
  nativefier
  npm
  speed-test
  yo
)

generators=(
  generator-devine-project
  generator-eslint-config
)

npm install -g ${modules[@]}
npm install -g ${generators[@]}
